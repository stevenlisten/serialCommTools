# 环境与工具登记表（规则 7：统一安装到 C:\tools\）

## 安装规范
1. 所有执行过程中需要安装的软件/工具一律安装到 `C:\tools\<工具名>\`。
2. 优先便携版/绿色版；可配置环境变量（如 PATH）。
3. 每次安装后必须在本文件登记：版本、路径、环境变量配置、验证命令。
4. 修改环境变量前记录旧值，修改后记录新值。

## 当前已登记
| 工具 | 版本 | 路径 | 环境配置 | 验证命令 | 登记日期 |
|------|------|------|----------|----------|----------|
| MinGW (mingw32) | <见验证输出> | C:\tools\mingw32 | 未配置（如需 gcc 请配置 PATH 或直接调用绝对路径） | gcc --version | 2026-08-02（发现时已存在） |

验证输出（登记时执行）：
```
' + $gccVer + '
```

## 变更日志
- 2026-08-02：初始化本文件；登记已有工具 mingw32；约定后续安装一律登记于此。